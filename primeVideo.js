/*
function skip() {
  // recap button
  const recapButton =  document.querySelector("")
  
  // skip button 
  const introButton = document.querySelector("")
  
  // next episode
  const nextEpisode = document.querySelector("")
  
  if (recapButton) {
    recapButton.click();
  } 
  if (introButton) {
    introButton.click();
  }
  if (nextEpisode) {
    nextEpisode.click();
  }
}
  
setInterval(function() {
  skip();  
}, 1000);

*/